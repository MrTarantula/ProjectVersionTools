{
  "name": "setprojectversion",
  "version": "1.2.0",
  "description": "package.json for task lib",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "vsts-task-lib": "^2.7.0",
    "xmldom": "^0.1.27"
  }
}
