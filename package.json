{
  "scripts": {
    "package": "npm run clean  && npm run compile  && tfx extension create",
    "gallery-publish": "tfx extension publish",
    "clean": "rimraf ./*.vsix",
    "compile": "tsc -p ."
  },
  "devDependencies": {
    "@types/node": "^8.10.37",
    "@types/q": "1.5.1",
    "@types/xmldom": "^0.1.29",
    "tfx-cli": "^0.6.3",
    "typescript": "^2.9.2"
  },
  "name": "projectversiontools",
  "private": true,
  "version": "1.2.1"
}
